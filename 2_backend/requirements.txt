# PhishGuard API — Python Dependencies
# ======================================
# Install: pip install -r requirements.txt
# Python:  >= 3.9 required (asyncio.to_thread, str.removeprefix)

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0          # standard = websockets + httptools for speed

# Data validation
pydantic>=2.0.0

# Async HTTP client (used in production CTI stubs — install now, enable later)
aiohttp>=3.9.0

# DNS forensics — async resolver via dns.asyncresolver
dnspython>=2.4.0

# WHOIS forensics
python-whois>=0.9.0

# ML model persistence and inference
joblib>=1.3.0

# Numerical feature arrays
numpy>=1.24.0

# Optional: scikit-learn for training / loading real models
# Uncomment when you have a real phishguard_model.pkl
# scikit-learn>=1.3.0
